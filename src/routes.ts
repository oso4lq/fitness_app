enum Routes {
  Main = "/",
  Profile = "/profile",
  Course = "/course",
  Workout = "/workout",
  Login = "/login",
  SignUp = "/signup",
  ResetPassMail = "/reset-pass-mail",
  ResetPass = "/reset-pass",
  SelectWorkout = "/select-workout",
  MyProgress = "/my-progress",
  ApprovalResult = "/approval-result",
}

export default Routes;
